<h1>Accounts on this Server</h1>

<ul>
  <% @accounts.each do |account| %>
    <li><%= link_to account.resto_name, root_url(subdomain: account.subdomain) %></li>
  <% end %>
</ul>

<br>

<%= link_to 'New Account', new_account_path %>
